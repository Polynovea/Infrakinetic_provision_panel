# 1A.1 — Repository and environment foundation — status

Date: 2026-09-11 (fresh reconciliation 2026-09-16 — see the block immediately below; the original 2026-09-11 record is left intact beneath it, per this project's evidence-discipline convention of not overwriting a prior state's own record)

## 2026-09-16 reconciliation

```
OVERALL:          CLOSED, WITH ONE OPEN ITEM — see below
SOURCE:            COMPLETE — unchanged since 2026-09-11
GIT:               Pushed to github.com/Polynovea/Infrakinetic_provision_panel (origin/master), CI green on
                    every real commit through 85f7e03 (verified live via `gh run list` — 5/5 most recent runs
                    `completed success`). Branch protection is **NOT enabled** (`gh api
                    repos/.../branches/master/protection` → 404 "Branch not protected") — this is the one
                    open item against the master plan's own §7.2 exit criterion ("Repo branch-protected, CI
                    green on a real commit"); CI-green is proven, branch-protected is not. CODEOWNERS now
                    names a real reviewer (`@subrojitroy10`), no longer the placeholder the 2026-09-11 record
                    below describes.
DATABASE:          N/A for this subphase — no DB code exists here by design (see 1A.3).
RUNTIME:           LIVE — verified this session by direct HTTP request, not inferred from other phases' docs:
                    `https://governance.infrakinetic.in/` → 200 (frontend, Vercel/DNS live); `GET
                    https://governance-api.infrakinetic.in/healthz` → 200
                    `{"status":"ok","service":"polynovea.platform-governance"}` (backend, EC2/PM2/nginx live).
                    Both were recorded in the 2026-09-11 table below as "Not yet" — both are now real.
SECURITY:          Zero Infrakinetic source imported (grep-provable, unchanged). Domain-portability CI check
                    still wired and still passing on every commit above.
BROWSER:           Live frontend bundle fetched and grepped directly (`_next/static/chunks/*.js` from the real
                    deployed site): contains `governance-api.infrakinetic.in` only — zero occurrences of
                    `governance-api.polynovea.in` or any bare `polynovea.in` literal. Satisfies this
                    reconciliation pass's own check and doubles as advance evidence for the master task's
                    "live frontend bundle contains no governance-api.polynovea.in" requirement.
EXTERNAL/CLOUD:    GitHub repo, Vercel project + DNS, and the EC2/PM2 backend process are all live (see RUNTIME
                    above) — all three were "not yet" in the 2026-09-11 record.
OPEN ITEMS:        Enable GitHub branch protection on `master` (require the existing CI checks, at minimum;
                    CODEOWNERS review since a real reviewer is now named). This is a repository security
                    setting change — I'm not making it myself; it needs to be done from the GitHub UI or via
                    `gh api repos/Polynovea/Infrakinetic_provision_panel/branches/master/protection --method
                    PUT ...` by someone with admin rights on the repo.
```

## Original 2026-09-11 record (historical — see reconciliation above for current state)

## Done, local only

- Repository skeleton created at this path, `git init`'d locally, not yet pushed to any remote.
- `/frontend` (Next.js 14 / TypeScript / App Router placeholder — no auth, no real functionality, no `NEXT_PUBLIC_*` vars declared).
- `/backend` (Express / TypeScript, `GET /healthz` only, bound to `127.0.0.1:4100` by default, every other route 404s, zero database dependency).
- `/docs` (this file).
- `README.md` stating purpose and the 10 hard boundary rules.
- `CODEOWNERS` (placeholder username — needs a real reviewer set before this has any effect).
- `.github/workflows/ci.yml` — lint + typecheck + build for both packages, plus the domain-portability grep check. No deploy step.
- `backend/scripts/check_domain_portability.mjs` — fails on any hardcoded `polynovea.in`/`infrakinetic.in` literal outside `.env*`, `README.md`, `/docs`, and the check itself.
- `backend/ecosystem.config.cjs` — PM2 process definition (`polynovea-governance-api`, separate port/logs/cwd from `polynovea-api`). **Not deployed anywhere.**
- Zero Infrakinetic source code imported — this repo has no dependency on, or filesystem reference to, the Infrakinetic repository.

## Deliberately not done — each needs its own separate, explicit authorization

Per the master plan's own §7.1 framing ("still not authorized to begin without your go-ahead on this exact scope") and this session's explicit instruction to keep EC2/RDS out of Phase 1A development:

- **GitHub remote repository creation.** Nothing has been pushed anywhere; this exists only as a local git repository. Branch protection and CODEOWNERS enforcement require the repo to exist on GitHub first.
- **Vercel project / custom domain (`governance.infrakinetic.in`) attachment.** Requires Vercel account access and, separately, a DNS record — explicitly flagged in the scoping doc (§9) as needing its own authorization, distinct from repo creation.
- **Any EC2 change.** No new working directory, PM2 process, or nginx vhost has been created on the existing EC2 host. `ecosystem.config.cjs` exists in this repo as configuration, not as an applied change.
- **CI has not actually run.** The workflow file exists locally; it has never executed on GitHub's infrastructure (same operational-certification-pending status as Phase 12's own CI gate).

## Exit gate — status against the master plan's §7.2 checklist

| Item | Status |
|---|---|
| Repo branch-protected, CI green on a real commit | Not yet — repo not pushed |
| Frontend live at `governance.infrakinetic.in` | Not yet — no Vercel/DNS |
| Backend PM2 process bound to `127.0.0.1:4100` only | Config exists; not deployed |
| `governance-api.infrakinetic.in/healthz` returns 200 | Not yet — no EC2/nginx |
| `~/governance/.env` vs `~/api/.env` zero overlap | N/A — no EC2 `.env` created yet |
| `/healthz` passes with unreachable `DATABASE_URL` | **True by construction** — no DB code exists in this subphase at all |
| Zero Infrakinetic source in this repo | **True, grep-provable** |
| Process isolation (separate PM2/cwd/logs) | Config written; not yet deployed to prove live |
| Rollback proven | N/A — nothing deployed to roll back |
| Domain-portability CI check proven to catch a real violation | **Done** — introduced `backend/src/_tmp_violation.ts` with a hardcoded `infrakinetic.in` literal, confirmed `exit=1`, reverted, confirmed clean `exit=0` |

## Local verification performed (2026-09-11)

- `backend`: `npm install` (221 packages), `npm run typecheck` clean, `npm run build` clean, `npm run lint` clean, built server started and `GET /healthz` returned `200 {"status":"ok","service":"polynovea.platform-governance","phase":"1A.1"}`; an unknown route returned `404`; server bound to `127.0.0.1:4100` only.
- `frontend`: `npm install` (331 packages), `npm run typecheck` clean, `npm run build` clean (`next build`, static-prerendered placeholder page, 4 pages generated, no errors).
- Domain-portability check: negative-case proof completed (see table above).

## Suggested next steps, in order

1. Decide and authorize, separately: GitHub repo creation, then Vercel project + `governance.infrakinetic.in` DNS/attachment, then (only when a real EC2 change is actually wanted) the backend deploy.
2. 1A.2 — privileged operator identity (dedicated Cognito pool) can begin once the repo exists on GitHub with branch protection live.

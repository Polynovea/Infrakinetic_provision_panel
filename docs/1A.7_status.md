# 1A.7 — Tenant registry — status

Date: 2026-09-14 (extended 2026-09-15; fresh reconciliation 2026-09-16 — see block immediately below; earlier records left intact after it)

## 2026-09-16 reconciliation

```
OVERALL:           SOURCE + LOCAL TESTS COMPLETE, GIT PUSHED, LIVE-AUTHENTICATED CERTIFICATION PENDING (tied to
                    the same live MFA'd operator session Phase B/1A.2 closure needs — see below)
SOURCE:             COMPLETE — unchanged since 2026-09-15
GIT:                The 2026-09-15 record below says "GIT COMMIT / PUSH: NOT DONE — awaiting your go-ahead."
                    That is now stale: both `4641c57` (tenant registry + operator-console frontend
                    reconciliation) and `9076945` (real DB-backed operator bootstrap + tenant-user inventory)
                    are committed and pushed to `origin/master`, confirmed via `git log`/`git status` this
                    session ("up to date with origin/master") and `gh run list` (CI green on every commit
                    through and after these two).
DATABASE:           N/A — this phase reads Infrakinetic's `tenants`/`app_users` through the Management API only,
                    never Governance's own DB directly.
RUNTIME:            Backend/frontend both live (see 1A.1's 2026-09-16 reconciliation for direct HTTP proof of
                    both domains). The specific `/management/v1/tenants` and `/management/v1/tenants/:id/users`
                    routes are deployed (present in the running backend build) but **not yet exercised live with
                    a real authenticated operator session** — that requires the same MFA'd login Phase B/1A.2
                    closure needs, not a new blocker of its own.
SECURITY:           Unchanged — sanitized field allow-list proven in tests (excluded fields never appear in the
                    SQL text), scope enforcement (`tenants.read`) proven in tests.
BROWSER:            The Tenants page was fully rebuilt (Phase D of the closure plan — search, status/plan/kind/
                    country filters, summary metric chips, a Drawer-based tenant detail view with Overview +
                    Users sections and a Technical-details disclosure for the tenant ID) and verified this
                    session in a real browser, both themes, both desktop and mobile width, against a throwaway
                    local mock backend serving the exact `/management/v1/tenants` and `/tenants/:id/users`
                    response shapes (same technique the 2026-09-15 record already used and documented — not
                    part of either repo, not committed). **Not yet** re-verified against a real Cognito session
                    and the real deployed Infrakinetic data — that is Phase G, alongside 1A.2's own live
                    certification.
EXTERNAL/CLOUD:     N/A beyond what RUNTIME above already covers.
OPEN ITEMS:         Live authenticated certification of `GET /management/v1/tenants` and
                    `GET /management/v1/tenants/:identifier/users` against the real deployed pair — deferred to
                    Phase G of the closure plan, alongside 1A.2's own live certification. Frontend UX redesign
                    itself (the "deliberately minimal" note below) is now done and locally verified — see above.
```

## 2026-09-15 extension — tenant-user inventory, and the real internal `Polynovea` tenant

Added `GET /management/v1/tenants/:identifier/users` on both sides, same discipline as the existing registry routes:
Infrakinetic's `tenantRegistry.js` gained `listTenantUsers()` (explicit allow-list — `id, full_name, email, role_key,
status, created_at, last_active_at`; no MFA field, since Cognito-side MFA state is not mirrored into `app_users` and
a live per-request Cognito call is not worth the new dependency for a routine list read), Governance's
`tenantRegistryQuery.ts` gained the matching `getTenantRegistryUsers()`, and the Tenants page's existing detail
panel now renders the real user table for whichever tenant is selected. Read-only, R0 (no ledger, no idempotency
key) — identical reasoning to the existing tenant-registry routes. All new tests pass on both sides (Infrakinetic
+4, Governance +2 operation tests +1 route-boundary test).

**The real internal `Polynovea` tenant now exists**, created through the existing `POST /provision-tenant` endpoint
(not direct SQL) with `send_invite: false` — this creates only the `tenants` row plus a `tenant_pending_admins`
record, deliberately not touching Cognito or `app_users`:

```
tenant_id: b836d877-4a0b-4ceb-b5d3-a153d17238fc
slug: polynovea
plan: enterprise
status: active
tenant_kind: customer   (same kind as every other real tenant — not 'platform', per the existing sentinel/real-tenant
                          distinction; satisfies the 1A.18 invariant of the same pipeline as a customer)
trial_ends_at: null
pending admin: subrojitroy@polynovea.in / Subrojit Roy (tenant_pending_admins, no Cognito user yet)
```

Confirmed via direct query: zero `app_users` rows for this tenant, and the pre-existing `Polynovea Test`
(`901fd512-c6fc-4c77-befb-bf12cb8ff5f0`, slug `polynovea-test`) is untouched.

**1A.8 (commission → suspend → resume → decommission via the Management API) does not exist yet** — zero
implementation found anywhere in either repo (checked directly, not assumed). The real tenant above was created via
the existing, already-governing `provision-tenant` mechanism (the same one every real customer tenant already goes
through), not a new 1A.8 lifecycle, per an explicit plan-owner sequencing decision to bring the internal tenant
forward rather than wait on a from-scratch 1A.8 build. 1A.8 itself remains open, unstarted, substantial (new
Management API mutation routes, a state machine, ledger integration, idempotency, desired/effective-state evidence)
— a separate, later effort.

**Not done, and not this session's to do:** creating the actual Cognito user for `subrojitroy@polynovea.in` bound to
this tenant as `master_admin` (via `POST /admin/tenants/:id/users`, which also seeds `app_users` in the same call —
account creation is a hard operating boundary this session does not cross even under explicit authorization, per
`docs/1A.2_status.md`).

## Status (do not infer CLOSED from source completeness alone)

```
SOURCE IMPLEMENTATION (both repos):   COMPLETE
LOCAL FOCUSED TESTS:                  COMPLETE — 9 new Governance tests (6 tenantRegistryQuery, 3 route boundary),
                                       26 Infrakinetic tests (index.test.js, was 17, now includes 9 new 1A.7 cases)
FULL LOCAL TEST SUITE (Governance):   227/227
FULL LOCAL TEST SUITE (Infrakinetic): 302 files passed / 2 failed / 7 skipped (311); 2409 tests passed / 2 failed /
                                       64 skipped (2475) — correct root-scoped command below. The 2 failures are
                                       proven, with an isolated worktree, to be caused by concurrent uncommitted
                                       Document Engine work, not by 1A.7 — see "Isolation proof" below.
TYPECHECK / LINT / BUILD (Governance backend): CLEAN (tsc build, eslint src, eslint test, tsc --noEmit twice)
TYPECHECK / LINT / BUILD (Governance frontend): CLEAN (tsc --noEmit, next lint, next build — /, /tenants,
                                       /engine-state, /auth/callback all emit)
FRONTEND RECONCILIATION:              DONE — real operator-console shell, real Cognito Hosted UI auth flow
                                       (code-complete, not live-verified — no pool provisioned), zero internal
                                       jargon in product UI (grep-verified) — see "Frontend reconciliation" below
CRM CONFORMANCE CHECKS:               CLEAN — check:engine-contract-ownership, check:engine-catalog
GIT COMMIT / PUSH:                    NOT DONE — awaiting your go-ahead (see below)
LIVE CERTIFICATION / DEPLOYMENT:      NOT DONE — this touches live production infra; not attempted without
                                       explicit confirmation (see "Not done yet" below)
OVERALL 1A.7:                         SOURCE + LOCAL TESTS COMPLETE, CAUSALITY-ISOLATED, NOT YET CLOSED
```

## Correction — the earlier "91 failed / 98 failed" claim was invalid evidence

You caught a real methodology error in the prior version of this document. The earlier run was `cd api-server && npx
vitest run`, and some test files compute paths via `path.join(process.cwd(), "api-server", ...)` — correct when run
from the repo root, but doubled to `.../CRM/api-server/api-server/...` when `process.cwd()` is already `api-server`.
That produced 91 spurious `ENOENT` failures that do not reflect real state. The correct invocation, from the CRM
repository root, is:

```
npx vitest run api-server
```

which produces **302 passed files / 2 failed / 7 skipped, 2409 passed tests / 2 failed / 64 skipped**. This phase's
own file (`routes/management/v1/__tests__/index.test.js`) is 26/26 within that run. The prior "91/98" claim and the
"materially worse baseline than 1A.6" framing built on it are retracted.

## Isolation proof — the 2 genuine failures are not a 1A.7 regression

The two real failures against the current working tree are `api-server/src/lib/__tests__/employmentOfferCapability.test.js`
and `api-server/src/migration/__tests__/platformCatalogBuilder.test.js`. Per your instruction, causality was
established with an isolated worktree rather than argued from inspection alone — the CRM working tree currently
carries ~68 unrelated, uncommitted Document Engine files, so any claim about what causes what needs the delta
mechanically separated from that noise, not just asserted.

**Method**: two `git worktree`s created detached at the current CRM HEAD (`7bca5ad`, the same commit `git status` at
this session's start showed as `HEAD`), with `node_modules` reused via filesystem junctions (no reinstall, no
dependency drift):

- `1a7-baseline` — clean HEAD, nothing else. No Document Engine dirt, no 1A.7 delta.
- `1a7-delta` — clean HEAD + **only** the exact 1A.7 delta copied in: `lib/tenantRegistry.js` (new),
  `routes/management/v1/index.js`, `routes/management/v1/__tests__/index.test.js` (both modified). Confirmed via
  `git status` inside that worktree that these three paths are the *only* diff from HEAD.

**Results**:

| Tree | `employmentOfferCapability.test.js` + `platformCatalogBuilder.test.js` |
|---|---|
| Current working tree (dirty, ~68 unrelated Document Engine files) | **FAIL** (2 failed, 28 passed) — reproduces your finding |
| `1a7-baseline` (clean HEAD, no 1A.7) | **PASS** (30/30) |
| `1a7-delta` (clean HEAD + 1A.7 delta only) | **PASS** (30/30 for these two files; 56/56 including this phase's own 26-test file run alongside them) |

**Root cause, identified directly rather than left as a guess**: `platformCatalogBuilder.test.js` compares a freshly
generated migration destination catalog against a committed snapshot and fails on drift —
`tableCount: 508` (generated) vs `506` (committed snapshot), `missingAdapterCount: 17` vs `15`, including a
`dsp_deliveries` table absent from the committed snapshot. This is exactly the shape of a concurrent, uncommitted
Document Engine migration having been added without its derived catalog snapshot being regenerated to match — a
real, pre-existing gap in the *current dirty tree*, proven absent from both the clean committed baseline and from
1A.7's own delta.

**Conclusion**: the two failures exist only in the current dirty working tree, are absent from clean git history, and
are absent from clean history plus 1A.7. They are concurrent-work artifacts, not a 1A.7 regression and not a
regression against committed history either. Per your instruction ("if the two failures exist without 1A.7, record
them as baseline/concurrent-work failures with proof") — they don't even reach "baseline": they don't exist in clean
committed history at all, only in the uncommitted concurrent work sitting on top of it.

**A methodology note, for completeness, not a finding that changes the conclusion above**: a full unfiltered
`npx vitest run api-server` inside the *clean* `1a7-baseline`/`1a7-delta` worktrees was noisier than the two-file
isolation above — 3 different files failed on one run and a different 3 on a repeat, none of them
`employmentOfferCapability`/`platformCatalogBuilder`. Investigated rather than waved off: those failures
(`financeReportsPhase6.test.js`, `tenantIsolationRemediation.test.js`, `phase7SchemaContract.test.js`) turned out to
depend on files that are `.gitignore`d (`docs/` is ignored wholesale per `.gitignore:24`, and this repo keeps a small
set of force-added exceptions — the specific doc these tests need isn't one of them) or on migration-file glob
resolution that differs when uncommitted local files aren't present. Both are pre-existing checkout-fragility
properties of a **from-scratch clean checkout** specifically (present in `1a7-baseline` too, i.e. with zero 1A.7
involvement), not something the real development/CI baseline ever hits, since the real working tree always has these
locally-present files. This is why the **302/2/7** figure above — measured against the actual working tree, exactly
as you specified — is the correct exit-gate number, not a number from the clean-worktree scaffolding, which exists
only to isolate 1A.7's own causal effect.

## Frontend reconciliation

> **Superseded (2026-09-14, commit `786e002`):** the client-side Cognito Hosted UI Authorization Code + PKCE flow
> described in this section — `lib/cognitoAuth.ts`, `lib/pkce.ts` performing the OAuth exchange in the browser, and a
> Cognito ID token held client-side and attached as `Authorization: Bearer` on every call — is **not** the canonical
> browser session model and has been replaced. `lib/cognitoAuth.ts` and `lib/pkce.ts` are deleted. The real,
> current architecture is a backend-owned opaque session: the browser never performs any part of the OAuth exchange
> and never holds a Cognito ID/refresh token; `backend/src/routes/auth` runs the full Authorization Code + PKCE
> exchange server-side (state/nonce/verifier checked backend-side against `oauth_login_transactions`, migration
> `0004`) and issues its own opaque, hashed-at-rest session as an HttpOnly cookie plus a CSRF token
> (`requireBrowserCsrf`) for state-changing requests. `session.tsx` now just asks the backend for session status.
> This section is left below as the historical record of what was actually built and locally verified at the time —
> it was real, working code, just not the locked topology — not as current direction.

This went through two passes. The first pass (a nav shell naming phase numbers) was superseded mid-review by your
follow-up: the product UI should not expose programme/engineering language at all, and the pasted-token field should
not be the normal production authentication experience. What's actually built now:

**Product surface** — a real operator-console shell (`components/AppShell.tsx`): sidebar (Overview / Tenants /
Platform), topbar with the signed-in operator's real email and a sign-out control, all three real pages
(`app/(product)/page.tsx`, `tenants/page.tsx`, `engine-state/page.tsx`) rebuilt against a shared design system
(`app/globals.css`: CSS custom properties, light/dark via `prefers-color-scheme`, a data table, status badges,
cards, form fields, loading/empty/error state components) instead of page-by-page inline styles. No product-facing
text names a phase number, a master-plan section, a doc path, or an env var — verified by grep across
`app/(product)/**` and `components/**`, zero matches. Engine state, tenant status, and tenant kind render as plain
badges ("Operational" / "Degraded" / "Disabled", "Active" / "Customer" / "Platform"); tenant reads show
"Last observed \<timestamp\>" instead of a freshness-envelope field name. The "Operations/Audit" shell section named
in your message was deliberately **not** added: the only real backend capability there is looking up one already-
known operation by id (`GET /operations/:operationId`) — there is no listing endpoint — so a nav item would point at
nothing. That single-operation view already surfaces in place, on the Platform page, right after a state change.
The Tenants page also gained a real drill-down (click a row → detail panel via the already-built
`GET /tenants/:identifier`) that wasn't in the original 1A.7 page — a genuine existing backend capability now
surfaced, not new functionality.

**Authentication** — the pasted-token field is no longer the product's normal path.
`lib/cognitoAuth.ts`/`lib/pkce.ts`/`lib/session.tsx` implement a real Cognito Hosted UI Authorization Code + PKCE flow
against the documented OAuth2/OIDC contract: `signIn()` redirects to `{domain}/login` with a PKCE challenge,
`app/auth/callback/page.tsx` exchanges the returned code for a real ID token at `{domain}/oauth2/token`, and every
subsequent call attaches that token as `Authorization: Bearer`. Configuration (`NEXT_PUBLIC_GOVERNANCE_COGNITO_DOMAIN`
/ `_CLIENT_ID` / `_REDIRECT_URI`) is read only in `lib/authConfig.ts` and is absent from every user-visible string.
**Fails closed**: if that configuration is absent, the product shows exactly "Sign-in unavailable — Sign-in is not
available right now. Contact your platform administrator." — no fallback, no token field, verified in a real browser
against a real production build (`next build` with no Cognito/dev-auth env set, then loaded — confirmed via
screenshot and page text). A development-only bypass (`components/DevSignIn.tsx`, a pasted-token form) exists behind
**two** independent gates: `NODE_ENV !== "production"` (Next.js's own build-time constant — a real `next build`
statically eliminates this branch regardless of any other setting) and an explicit
`NEXT_PUBLIC_GOVERNANCE_DEV_AUTH=1` opt-in that defaults unset. It is visually marked as a developer tool (dashed
warning-colored panel, "Development sign-in — not available in production builds"), never phrased as a product
feature. Verified in a real dev server with the flag set: the bypass form renders (and only the bypass form — no
"Sign in" button, since no Hosted UI is configured); with the flag unset, only the fail-closed screen renders.

**What was verified live, and what wasn't, stated honestly**: with the dev bypass and a throwaway local mock of
`/management/v1/whoami` and `/management/v1/tenants` (a scratch script, not part of either repo), the full
authenticated shell was exercised in a real browser — sidebar, topbar with real operator email, real tenant count on
Overview, the Tenants table with working row drill-down, the Platform form — in both dark and light color schemes and
at mobile width. The real Cognito Hosted UI redirect and token exchange in `lib/cognitoAuth.ts` were **not** exercised
against a live pool — `GOVERNANCE_COGNITO_*` has never been provisioned in any real AWS environment (the same standing
gap every prior status doc names), so no Hosted UI exists yet to redirect to. This is an infrastructure gap, not a
code gap: provisioning a real Cognito user pool is outside what a coding session can or should do unilaterally. The
flow is written against Cognito's real, documented Hosted UI contract and is ready to exercise the moment a pool and
app client exist; that readiness claim is unverified until they do.

Typecheck/lint/build clean (`tsc --noEmit`, `next lint`, `next build` — `/`, `/tenants`, `/engine-state`,
`/auth/callback` all emit).

## Scope grounding

See `docs/1A.7_scoping.md` for the full ground-truthing pass (real `tenants` table columns, why no reusable read
primitive existed, scope reuse, and the three data-exposure decisions you locked before implementation started:
sanitized field list, platform-tenant visibility, id-or-slug lookup).

## What was built

**Infrakinetic (`CRM/api-server/src/`):**

- `lib/tenantRegistry.js` (new) — `listTenants()` / `getTenant(identifier)`. Explicit column allow-list (never
  `SELECT *`), using `bypassQuery` — the same RLS-bypass connection `admin.js`'s own `GET /admin/tenants` already
  uses for this exact table, for the same reason (a cross-tenant registry legitimately spans every tenant). Resolves
  by `id` when the path segment is a valid UUID, otherwise by exact `slug`.
- `routes/management/v1/index.js` — two new routes: `GET /tenants` (scope `tenants.read`, action
  `tenants.registry.read`) and `GET /tenants/:identifier` (scope `tenants.read`, action
  `tenants.registry.detail.read`, 404 `UNKNOWN_TENANT` on a miss). Both responses carry the master-plan-§61 freshness
  envelope (`observedAt`, `source: "infrakinetic-live"`, `freshness: "live"`) — no cache exists in this phase, so this
  is simply the read timestamp.
- `routes/management/v1/__tests__/index.test.js` — 9 new tests: successful list with the exact sanitized shape and a
  positive assertion that excluded fields (`admin_email`, `admin_name`, `razorpay_*`, `attendance_import_token`) never
  appear in the SQL text; wrong scope; wrong requested action; no assertion; lookup by UUID; lookup by slug (with the
  canonical `id` still returned); unknown identifier (both UUID-shaped and slug-shaped misses return the identical
  404 body).

**Governance (`backend/src/`):**

- `management/operations/tenantRegistryQuery.ts` (new) — `listTenantRegistry()` / `getTenantRegistryEntry()`. Mirrors
  `engineStateOperation.ts`'s mint-then-call shape, minus idempotency/ledger/verify-after-mutation, which don't apply
  to a pure read. No `management_operations` row is created for these calls (R0 per 1A.5's own risk classification —
  the ledger exists to make mutations safely retryable and durably auditable, neither of which a read needs); authn/
  authz denial is still audited through the existing `requireManagementApiAuth`/`requireScope` perimeter, same as
  every other route. `engineStateOperation.ts`'s `MANAGEMENT_V1_PREFIX` constant was exported (previously module-local)
  so this file reuses it instead of redeclaring it.
- `routes/management/index.ts` — `GET /tenants`, `GET /tenants/:identifier`, both behind
  `requireScope("tenants.read", deps.auditSink)`. No new `ManagementRouterDeps` field was needed — signing
  keys/transport config/base URL were already there for 1A.6. Updated the file's own header comment, which had
  described `/tenants` as still belonging to "1A.4's read-only contract deliverable" — it's now actually wired here.
- `management/operations/tenantRegistryQuery.test.ts` (new) — 6 tests against a fake Infrakinetic with an injectable
  `fetchImpl` (same technique as `engineStateOperation.test.ts`): list returns the sanitized shape untouched with the
  freshness envelope, exactly one assertion minted per call; an operator missing `tenants.read` in their granted
  scopes throws before any HTTP call is attempted; unexpected upstream status maps to
  `UnexpectedManagementApiResponseError`; detail lookup by id; detail lookup by slug (canonical id still returned);
  unknown identifier maps to `UnknownTenantError`.
- `routes/management/tenantRegistryRoute.test.ts` (new) — 3 tests for the route's own boundary (matching
  `engineStateRoute.test.ts`'s own framing: this route has no fetch-injection point, matching production, so only
  auth/scope is exercised here — the real orchestration is covered above): missing `tenants.read` scope rejected
  before any orchestration on both routes; a viewer-role operator (who does hold `tenants.read`) is not rejected on
  scope; no assertion at all → 401.
- `frontend/app/tenants/page.tsx` (new) — deliberately minimal, matching `engine-state/page.tsx`'s own posture (pasted
  operator token, no real Cognito login yet, calls only the Governance backend). Read-only table (name, slug, kind,
  plan, status, country, created) plus an "observed at" banner. No commission/suspend controls — 1A.8's deliverable,
  not this phase's.

## Scopes/actions used this phase

Only `tenants.read` (already in the 1A.2 catalog, unused until now — no new scope invented, same discipline as
1A.6's `engines.platform_state.write`). Two new per-route `requested_action` values, matching the existing
`engines.catalog.read`/`engines.state.read` split: `tenants.registry.read`, `tenants.registry.detail.read`.

## Not done yet — needs your decision before proceeding

Per this project's own risk posture (touching live/shared infrastructure needs an explicit go-ahead, not an
assumption), none of the following were done:

1. **Git commit/push**, either repo.
2. **Live certification** — a real `GET /management/v1/tenants` against the real deployed Infrakinetic, proving the
   real tenant set comes back sanitized (grep proof, same technique 1A.5 used) and the freshness envelope is present
   and correct, matching 1A.5/1A.6's own certification rigor.
3. **Deployment** — neither `polynovea-governance-api` nor `polynovea-api` was touched; both are still running
   whatever was live at the end of 1A.6.

Tell me whether to proceed with commit → live certification → deploy (the same sequence 1A.5/1A.6 followed), or stop
here with source + local tests complete and pick this back up separately.

## Direct answers to your four questions

> **Superseded (2026-09-14, commit `786e002`):** answers 1 and 2 below describe the client-side Cognito Hosted UI
> flow that was replaced by the backend-owned opaque-session architecture — see the superseding note under "Frontend
> reconciliation" above. Answers 3 and 4 (dev-only bypass gating, no engineering text in product UI) are unaffected.

1. **What operator-auth path does the frontend now use?** A real Cognito Hosted UI Authorization Code + PKCE flow,
   client-side only (no backend-for-frontend): `signIn()` redirects to the configured Hosted UI domain with a PKCE
   challenge; `/auth/callback` exchanges the returned code for a real ID token; every API call attaches that token as
   `Authorization: Bearer`. Configuration lives only in `lib/authConfig.ts`, never in product-facing text.
2. **Is Cognito/browser session wiring genuinely complete?** Code-complete against the real, documented Cognito
   Hosted UI contract; **not** live-verified. No real AWS Cognito user pool has ever been provisioned in any
   environment — the same standing gap named in `docs/1A.2_status.md`/`docs/1A.6_status.md`. That's an infrastructure
   provisioning gap, not a code gap, and provisioning real AWS identity infrastructure is outside what this session
   can or should do unilaterally. The flow will either work as written or reveal a real integration bug the moment a
   pool exists — that has not been tested.
3. **What, if anything, remains development-only?** `components/DevSignIn.tsx`, the pasted-token bypass, gated by
   `NODE_ENV !== "production"` (a `next build` statically eliminates this branch) **and** an explicit
   `NEXT_PUBLIC_GOVERNANCE_DEV_AUTH=1` opt-in that defaults unset. Verified both ways in a real browser: unset →
   fail-closed screen only; set → the bypass form only (no real "Sign in" button, since Cognito isn't configured
   either). Also development-only: the mock `/management/v1/whoami`+`/tenants` server used to visually verify the
   authenticated shell — a throwaway scratch script, not part of either repository.
4. **Does any engineering/internal text remain visible in production UI?** No — grepped `app/(product)/**` and
   `components/**` for phase numbers, `§` section references, `docs/` paths, and `GOVERNANCE_COGNITO_*`: zero matches.
   The only places that vocabulary still exists are source comments (never rendered) and this status document
   (engineering documentation, not product UI).
